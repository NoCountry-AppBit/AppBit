import Sidebar from "../components/sidebar";

const Home = () => {
    return (
        <div className="flex h-screen overflow-hidden bg-gray-100">
            <Sidebar role="candidato" />
        </div>
    );
};

export default Home;
